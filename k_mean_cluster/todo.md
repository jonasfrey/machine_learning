update the k-means so that it works with any dimension of data currently it's only 1D


create a small website where i can use a canvas and spray paint random points on it to get some nice manual testdata. 
 


 create a small website that uses a distance sign field of shapes, like stars, hearts, and or primitves, circles, squares. then generate testdata with 30 points each . treat the points as 30 dimensional vectors and then run the k-means on the data to re-discover the shapes. 
 